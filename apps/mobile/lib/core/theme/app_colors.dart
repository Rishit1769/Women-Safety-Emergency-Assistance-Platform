import 'package:flutter/material.dart';

/// RakshaAI Design Token System — mirrors UIUX.txt color specification

class AppColors {
  AppColors._();

  // ─── Primary ─────────────────────────────────────────────────
  static const Color primary        = Color(0xFFFF6B2D);
  static const Color primaryLight   = Color(0xFFFF8A4D);
  static const Color primaryDark    = Color(0xFFE85213);

  // ─── Emergency ───────────────────────────────────────────────
  static const Color emergency      = Color(0xFFD72638);
  static const Color emergencyLight = Color(0xFFFF4D5E);
  static const Color emergencyDark  = Color(0xFFA81828);

  // ─── Safe ─────────────────────────────────────────────────────
  static const Color safe           = Color(0xFF4CAF50);
  static const Color safeLight      = Color(0xFF81C784);
  static const Color safeDark       = Color(0xFF388E3C);

  // ─── Navy ─────────────────────────────────────────────────────
  static const Color navy           = Color(0xFF0B1026);
  static const Color navyLight      = Color(0xFF1A2240);

  // ─── Utility ─────────────────────────────────────────────────
  static const Color aiPurple       = Color(0xFF7B61FF);
  static const Color warning        = Color(0xFFFFB020);
  static const Color background     = Color(0xFFF7F8FC);
  static const Color white          = Color(0xFFFFFFFF);
  static const Color border         = Color(0xFFE5E7EB);
  static const Color muted          = Color(0xFF7E8794);
}
